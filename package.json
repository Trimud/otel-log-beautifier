{
  "name": "otel-log-beautifier",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/vscode"
  ],
  "scripts": {
    "build": "npm run build -w packages/vscode",
    "test": "npm run test -w packages/core && npm run test -w packages/vscode",
    "test:core": "npm run test -w packages/core",
    "test:vscode": "npm run test -w packages/vscode",
    "package": "npm run build && cd packages/vscode && npx @vscode/vsce package --no-dependencies"
  }
}
